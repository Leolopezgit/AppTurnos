export const metadata = { title: 'HeGeS 360 | Turnos médicos', description: 'Reserva de turnos médicos' };

export default function RootLayout({ children }) {
  return <html lang="es"><body>{children}</body></html>;
}
